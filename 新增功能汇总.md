# kiro-rs-lyozc 新增功能汇总

> 本文档说明 **kiro-rs-lyozc** 相对**原版 kiro.rs v0.7.3** 新增的全部功能。
>
> **编制方法**：把原版仓库 checkout 到 `v0.7.3` 标签（二开的分叉点）导出成独立源码树，
> 与二开逐文件做 SHA-256 对比，再逐符号（struct / 字段 / 路由 / 函数）比对差集。
> 文中每条声明都来自这次比对，不含推测。
>
> **为什么必须对 v0.7.3 而非原版当前代码**：原版已发布 v0.7.4。直接和原版最新代码 diff
> 会把「原版 0.7.3→0.7.4 的新增」误算成「二开的改动」——实测这会让 `token_manager.rs`
> 呈现为 **-785 行**（看似二开删了大段代码），而对 v0.7.3 的真实结果是 **+394 行**。

---

## 目录

1. [一句话总览](#一句话总览)
2. [改动规模](#改动规模)
3. [一个必须先澄清的事实](#一个必须先澄清的事实)
4. [新增功能一：Token 膨胀倍率](#新增功能一token-膨胀倍率)
5. [新增功能二：多引擎缓存模拟](#新增功能二多引擎缓存模拟)
6. [新增功能三：上游 API 凭据直通](#新增功能三上游-api-凭据直通)
7. [新增功能四：逐引擎计费对比](#新增功能四逐引擎计费对比)
8. [新增功能五：用量与链路的计费维度](#新增功能五用量与链路的计费维度)
9. [新增功能六：原子写文件](#新增功能六原子写文件)
10. [配置文件新增项](#配置文件新增项)
11. [API 新增清单](#api-新增清单)
12. [数据结构新增清单](#数据结构新增清单)
13. [前端新增清单](#前端新增清单)
14. [文件级改动全表](#文件级改动全表)
15. [测试覆盖](#测试覆盖)
16. [兼容性说明](#兼容性说明)

---

## 一句话总览

原版已能**模拟缓存命中**（把真实 token 总量拆成 `input / cache_creation / cache_read`）。
二开在此之上新增：

| # | 功能 | 一句话 |
|---|---|---|
| 1 | Token 膨胀倍率 | 原版只拆分总量、不改总量；二开可把总量**乘大** |
| 2 | 多引擎缓存模拟 | 从 1 个引擎扩到 **4 个**，按客户端 Key 选择 |
| 3 | 上游 API 凭据直通 | 凭据池可放**另一个 Anthropic 兼容端点**，原样转发 |
| 4 | 逐引擎计费对比 | 「上游真实成本 ↔ 客户端被计费」按引擎配对统计 |
| 5 | 用量与链路的计费维度 | JSONL / SQLite 记录逐引擎计费快照 |
| 6 | 原子写文件 | 缓存状态落盘改为 temp+rename，防掉电截断 |

---

## 改动规模

对 v0.7.3 基线的实测结果：

```
新增文件      18 个
删除文件       0 个        ← 二开是纯增量，未删除任何原版文件
修改文件      41 个
未改动文件   121 个
源码净增   +5903 行（.rs / .ts / .tsx）
测试数      537 → 658（+121）
```

### 新增的源码文件

| 文件 | 行数 | 内容 |
|---|---|---|
| `src/anthropic/cache_metering_go.rs` | 2270 | 引擎 B 完整实现（移植自 kiro-go） |
| `src/anthropic/cache_engine.rs` | 1312 | 四引擎统一接口层 |
| `src/anthropic/upstream.rs` | 853 | 上游 Anthropic 直通处理器 |
| `src/common/fs.rs` | 96 | 原子写文件 |

### 新增的前端文件

| 文件 | 行数 | 内容 |
|---|---|---|
| `admin-ui/src/components/cache-engine-dialog.tsx` | 394 | 四引擎参数弹窗 |
| `admin-ui/src/components/billing-comparison.tsx` | 220 | 逐引擎计费对比页 |
| `admin-ui/src/components/ui/table.tsx` | 117 | 表格基础组件 |
| `admin-ui/src/api/billing.ts` | 40 | 计费 API 客户端 |
| `admin-ui/src/hooks/use-cache-engines.ts` | 38 | 引擎配置 / 统计 hook |
| `admin-ui/src/hooks/use-billing.ts` | 33 | 计费 hook |

---

## 一个必须先澄清的事实

**模拟缓存的核心机制是原版功能，不是二开新增的。**

这点值得单独说明，因为项目里原有的 `FEATURES.md` 把「双引擎模拟缓存 token 膨胀」整体列为
二开的核心扩展，容易让人误以为模拟缓存是从零实现的。实测 v0.7.3 已具备：

| 原版 v0.7.3 已有 | 出现次数 |
|---|---|
| `compute_cache_usage`（分段 → 查命中 → 算 read/creation） | 31 处 |
| `struct CacheUsage` | 1 处 |
| `split_against_total`（按比例分摊到真实总量） | 9 处 |
| `isolation_seed`（会话隔离种子） | 2 处 |
| `spawn_background`（定期落盘） | 1 处 |
| `cache_metering.rs` 自带测试 | 22 个 |

而且原版已经把模拟值**下发给客户端**——`handlers.rs:1257` 与 `stream.rs:1420` 都在调
`split_against_total(total_input_tokens)`。

**原版与二开在这件事上的真实区别**：

```
原版：  真实总量 100 ──► 拆成 input=40 / creation=30 / read=30
                          总量守恒，客户端看到的总数仍是 100

二开：  真实总量 100 ──► 拆分（同上）──► 各项乘倍率
                          input=40×2  creation=30×3  read=30×3
                          客户端看到的总数变成 260
```

原版**只拆分、不放大**；二开新增的是「放大」这一步（倍率），以及围绕它衍生的多引擎、
上游直通和计费对比。`inflation_multiplier` 在原版 **0 个文件**中出现，是纯新增概念。

对应地，`cache_metering.rs` 在本次比对中是**修改**（1393→1630，+237 行）而非新增，
新增的部分是：`CacheMeterStats`（运行计数器）、`apply_config`（热重载）、
`multipliers`（引擎 A 的四个倍率 + 全局回退）、`is_simulated`（P1 修复）、
`MULTIPLIER_UNSET` 哨兵、以及改用 `write_file_atomic` 落盘。

---

## 功能一：Token 膨胀倍率

原版把模拟拆分的结果原样下发，总量守恒。二开在下发前对每一项乘一个可配倍率，使客户端
看到的 token 数高于真实值。

### 全局倍率（三个）

```jsonc
{
  "inputInflationMultiplier": 1.0,   // 作用于 input_tokens
  "outputInflationMultiplier": 1.0,  // 作用于 output_tokens
  "cacheInflationMultiplier": 1.0    // 作用于 cache_read + cache_creation
}
```

三个字段都是 `#[serde(default = "default_inflation_multiplier")]`，缺省 1.0（不放大），
所以老配置文件不改也能跑，行为与原版一致。

约束 `[1.0, 100.0]`，在 `TokenManager::set_token_inflation_config` 中校验，越界返回 400
并回滚原值。**下限是 1.0**——不允许把 token 数改小。

### 热更新

倍率存为 `AtomicU64`（f64 的 bit 表示），改配置后立即生效，不需重启：

```
PUT /api/admin/config/token-inflation
  → 校验 [1.0, 100.0]
  → 落盘 config.json
  → store 到三个原子量
  → 失败时回滚全部三个
```

`TokenManager` 新增的 4 个方法全部服务于此：`get_input_inflation_multiplier`、
`get_output_inflation_multiplier`、`get_cache_inflation_multiplier`、
`set_token_inflation_config`。

### 流式路径的接入

`stream.rs` 新增 4 个字段与 2 个方法（`set_inflation_multipliers`、
`set_inflation_multipliers_split`）。后者是四元组版本——因为引擎的 `cache_read` 与
`cache_creation` 可以取不同倍率，而全局倍率里这两项共用一个值。

---

## 功能二：四引擎模拟缓存

原版只有一套模拟算法。二开扩成四套，**按客户端 Key 独立选择**，可在同一部署内并存对比。

```
客户端 Key 的 cacheEngine 字段
  "rust"     → 引擎 A（原版算法 + 新增倍率）
  "go"       → 引擎 B（移植自 kiro-go）
  "real"     → 引擎 C（不模拟，用上游真值）
  "nocache"  → 引擎 D（强制无缓存 + 本地估算）
  缺失       → 默认 "rust"（老数据兼容）
```

切换在 Key 级别生效，改完立即作用于下一个请求，无需重启。

### 四引擎对照

| | A · rust | B · go | C · real | D · nocache |
|---|---|---|---|---|
| 是否模拟缓存 | 是 | 是 | 否 | 否（恒为 0） |
| 缓存状态 | 有指纹表 | 有指纹表 | 无 | 无 |
| 隔离粒度 | session / Key | Kiro 凭据 | — | — |
| 写入时机 | `begin()` 即写 | `commit()` 成功后写 | — | — |
| input 来源 | 模拟分摊 | 模拟分摊 | 上游真值 | **本地估算** |
| output 来源 | 上游真值 | 上游真值 | 上游真值 | **本地估算** |
| 独立倍率 | 4 个（可回退全局） | 4 个 | 4 个 | 2 个 |
| 运行计数器 | 有 | 有 | 无 | 无 |

### 引擎 B — go 移植（+2270 行）

移植自 `kiro-go-lyozc` 的 `proxy/cache_tracker.go`，刻意与 Go 版本行为对齐，用于横向对比
两种算法的模拟效果。与引擎 A 的可观测差异：

| 方面 | 引擎 A | 引擎 B |
|---|---|---|
| 哈希输入 | 结构化文本签名 | 包装对象的规范 JSON（含结构字节） |
| Token 估算 | 仅原始文本字符 | 规范 JSON 全部字节 |
| HTML 转义 | 不转义 | 对齐 Go `json.Marshal`，`<>&` 转为 `<` 等 |
| 写入时机 | `begin()` 即读即写 | `commit()` 仅成功后写 |
| 隔离粒度 | 每 session / key_id | 每 Kiro 凭据（同凭据下不同 Key 共享） |
| 最深断点 | 不扫最后一个 | `scan_start = len-2`；仅 1 个断点时整段计 creation |
| 首轮上限 | 无 | 有 `max_ratio × total` 上限（默认 0.85） |

**两阶段写入**是引擎 B 相对 A 的一个实质改进：

```
请求开始  compute_for_account()  → 只读，不写指纹表
             ↓
请求成功  update_for_account()   → 才写入本轮断点
请求失败  不调用                  → 缓存状态不受污染
```

引擎 A 在 `begin()` 阶段即写，请求失败不回滚，后续重试可能产生幻命中（见「已知问题 P3」）。

### 引擎 C — real（不模拟）

直接采用上游返回的真实 `cache_creation_input_tokens` / `cache_read_input_tokens`，只套自己
那组倍率。无指纹表、无 TTL、无 `commit`，纯无状态换算。

用途是**对比基线**：当上游本身就是支持 prompt caching 的真实 Anthropic API 时，C 显示的是
未经模拟加工的原始缓存划分。

### 引擎 D — nocache（本地估算）

`cache_creation` / `cache_read` 恒为 0，且**完全不读上游 usage**：

```
input_tokens  = token::count_all_tokens(客户端请求)    ← 请求发出前即已确定
output_tokens = token::estimate_output_tokens(实际返回内容)
cache_*       = 0
```

**为什么不用上游的数**：上游可能本身就是另一个 kiro-rs 反代，它报的 usage 已经被模拟和
放大过一轮。拿它当"真值"会把上一跳的加工结果再加工一次。本地口径与上游无关。

流式路径的时序恰好成立：
- `input_tokens` 在流**开头**的 `message_start` 写 —— 本地算的，请求发出前已知
- `output_tokens` 在流**末尾**的 `message_delta` 写 —— 此时 delta 已全部累积完

累积器按 block index 重建 content 数组再整体估算一次，而不是逐 delta 分别计算求和。
原因是 `count_tokens` 非线性（<100 token 乘 1.5，≥800 乘 1.0），逐片段计算会让每个小
delta 都吃 1.5 倍而显著高估。重建后复用与非流式同一个 `estimate_output_tokens`，两条
路径**由构造保证同口径**。

## 三、逐引擎独立倍率

四个引擎各自持有完整的倍率组，**互不影响**。这是"并存对比"的前提：若共用一套倍率，
改一处会同时动两套，对比就失去意义。

### 各引擎的倍率维度

| 引擎 | input | output | cache_read | cache_create | 存储位置 |
|---|:-:|:-:|:-:|:-:|---|
| A · rust | ✓ | ✓ | ✓ | ✓ | `CacheMeter` 原子量 |
| B · go | ✓ | ✓ | ✓ | ✓ | `GoCacheTracker` 原子量 |
| C · real | ✓ | ✓ | ✓ | ✓ | `StatelessMultipliers`（共享 `Arc`）|
| D · nocache | ✓ | ✓ | — | — | `StatelessMultipliers`（共享 `Arc`）|

D 的 cache 两位不提供旋钮：它的 cache 恒为 0，乘任何数都是 0，留着只会让运维以为调它有用。
代码层面 `resolve()` 对 D 直接返回 `(input, output, 0.0, 0.0)`。

### 引擎 A 的倍率是 `Option`（向后兼容的关键）

A 的四个倍率**未设置时逐项回退全局膨胀倍率**：

| A 的字段 | 未设置时回退到 |
|---|---|
| `inputMultiplier` | `inputInflationMultiplier` |
| `outputMultiplier` | `outputInflationMultiplier` |
| `cacheReadMultiplier` | `cacheInflationMultiplier` |
| `cacheCreationMultiplier` | `cacheInflationMultiplier` |

**为什么必须是 `Option` 而不是默认 1.0**：老部署只配了全局倍率。若 A 的新字段直接默认
1.0，升级后已配的全局倍率会**静默失效**——比如 `input=2.0` 突然变成 1×，且没有任何报错。

同理，`sanitized()` 遇到非法值（NaN / ≤0）回落 `None` 而非 1.0：误配应退回"继承全局"，
不该静默改成"不缩放"。

底层实现：`CacheMeter` 把 f64 以 `to_bits()` 存进 `AtomicI64`，用 `-1` 作「未设置」哨兵
（`-1 as u64` 是负 NaN 位型，合法倍率撞不上）。回退在 `CacheMeter::multipliers()` 内完成，
到 `UsageMultipliers::Rust` 时四个值都已是具体数。

> 曾用千分比整数存（`(v * 1000.0).round()`），会把 <0.0005 的正倍率量化成 0。已改成位型，
> 见 [UPDATES.md](UPDATES.md)。

### 倍率不叠加

```
CacheEngines::begin(req, key_id, kind, global)
        │
        ├─ 定出 UsageMultipliers（四个变体之一，各携带完整四元组）
        │
        └─ .resolve() → (input, output, cache_read, cache_creation)
                        └─ 直接用于 scale()，不再乘任何其他倍率
```

每个请求只走**一个**引擎、只用**一套**倍率。`resolve()` 里是四个平行分支，没有相乘。

### 全局倍率的定位

`inputInflationMultiplier` 等三项现在有两个作用：

1. **引擎 A 未显式配置时的回退值**
2. **Kiro 路径的流式膨胀**（`stream.rs` 的 `set_inflation_multipliers`，但传入的已是
   `resolve()` 后的逐引擎值）

它不再是"所有引擎共用的倍率"。

---

## 四、上游 API 凭据直通

**新增文件**：`src/anthropic/upstream.rs`（853 行）
**修改**：`src/kiro/provider.rs`（+264）、`src/kiro/model/credentials.rs`（+29）

原版 v0.7.3 **完全没有**这个概念（那两处 `is_upstream` 是 `is_upstream_error`，错误分类，无关）。

### 用途

在凭据池里加入指向**另一个 Anthropic 兼容端点**的凭据（例如另一个 kiro.rs 反代实例、
或任何兼容中转）。请求以 Anthropic Messages API 格式**原样转发**，不做 Kiro 协议转换。

### 凭据新增字段

| 字段 | 说明 |
|---|---|
| `upstreamBaseUrl` | 上游端点基础 URL，如 `https://api.example.com` |
| `upstreamApiKey` | API Key，转发时作为 `x-api-key` 头 |

判定：`KiroCredentials::is_upstream_credential()`（`credentials.rs:496`）——
`upstream_base_url` 非空即为上游凭据。

### 路由分支

```
call_api_with_retry_for_credential()
  │
  ├── is_upstream_credential() == true
  │     ├── 目标：{upstreamBaseUrl}/v1/messages
  │     ├── 请求体：anthropic_body_raw（未经 Kiro 转换的原始 JSON）
  │     ├── 请求头：x-api-key / anthropic-version / anthropic-beta（透传）
  │     └── 响应交给 upstream.rs 处理
  │
  └── false → 原版 Kiro 协议路径（完全不变）
```

`provider.rs` 新增的四个 `pub fn`：

| 函数 | 作用 |
|---|---|
| `call_api_dual` | 非流式：按凭据类型走 Kiro 或上游 |
| `call_api_stream_dual` | 流式：同上 |
| `call_api_dual_for_credential` | 指定凭据（WebSearch 路径预判用） |
| `get_inflation_multipliers` | 供 `resolve()` 取全局倍率回退值 |

### 响应改写

`upstream.rs` 的三个改写点，全部收敛到 `UsageMode::resolve_tokens`：

```
非流式  handle_upstream_non_stream_response()
流式    inflate_sse_event()              → 下发给客户端的 usage
流式    update_stream_stats()            → 回报给 hook.record / tracer 的用量
```

三处共用同一套规则。若各写一遍，客户端看到的数字与用量日志会在引擎切换时悄悄分叉。

### 流式的时序

```
message_start        ← 写 input_tokens / cache_* （引擎 D 的 input 此时已知：本地算的）
content_block_delta  ← 引擎 D 在此累积输出文本
message_delta        ← 写 output_tokens（引擎 D 此时累积已完整）
```

未被识别的事件类型**原样透传**（`_ => event_text.to_string()`），只有上面两类会被重建。

### WebSearch 路径

`handle_websearch_request` 在路由前预先 acquire 一次凭据上下文以判断类型
（`token_manager` 新增的 `acquire_context_for`）：

- **上游凭据** → `call_api_dual_for_credential`，直接透传 `web_search` tool，由上游原生处理
- **Kiro 凭据** → 走原有 MCP WebSearch 路径

## 新增功能四：逐引擎计费对比

原版没有任何计费概念——`usage_stats.rs` 里没有 `TokenUsageBreakdown`，也没有 `/stats/billing` 端点。

### 要回答的问题

上游按**真实** token 收你钱，你按**膨胀后**的 token 向下游收钱。差额就是毛利。但要算清这个差额，两个口径必须来自**同一批请求**。

### v1 的结构性缺陷（本次已修）

第一版 schema 是三个平铺字段：

```
upstream_usage   ← 一个共享槽
rust_usage       ← 引擎 A 的模拟值
go_usage         ← 引擎 B 的模拟值
```

混合流量下（部分 Key 用 A、部分用 B），`upstream_usage` 累加的是**全部引擎的上游用量之和**，而 `rust_usage` 只含 A 的。相除得到的"加价倍数"分母里混着 B 的成本 —— **这个比值没有意义**。

引擎 C / D 更是无处安放：三槽只为 A / B 而设。

### v2 的逐引擎配对

```rust
struct EngineBillingPair {
    upstream: TokenUsageBreakdown,  // 该引擎流量的上游真实用量
    client:   TokenUsageBreakdown,  // 该引擎流量的客户端计费用量
    calls:    u64,
}
```

按 `(key_id, engine, credential_id, model)` 索引。两个口径**同一次请求同时记入**，故任意聚合层级上两者始终可比 —— 这是**由构造保证**的，不依赖两处逻辑保持同步。

### 稀疏性

该表只为 `is_upstream` 的记录建条目（Kiro 凭据没有美元成本，本就不参与计费对比）。所以条目数取决于**上游凭据数**，而非全部凭据数。

同时 `BucketStats` 移除了三个计费字段（它被复制到 8 张聚合表里，而计费只有 `query_billing` 读其中 2 张），单个 `BucketStats` 从约 160 字节降到约 56 字节。

### 记录字段

`UsageRecord`（JSONL 用量日志）新增：

| 字段 | 类型 | 含义 |
|---|---|---|
| `is_upstream` | `bool` | 是否走上游凭据（决定是否参与计费对比） |
| `engine` | `Option<String>` | 本次请求所用引擎 |
| `upstream_usage` | `Option<TokenUsageBreakdown>` | 上游真实四元组 |
| `client_usage` | `Option<TokenUsageBreakdown>` | 客户端被计费四元组 |
| `rust_usage` / `go_usage` | `Option<...>` | **v1 兼容字段，只读不写** |

`engine` 用 `String` 而非枚举，是有意的：JSONL 载入是 `if let Ok(rec)`，**解析失败会静默丢弃整条记录**。若用枚举，将来新增引擎后用旧二进制读那批日志，会连 token 数一起丢失，而不只是丢个引擎标签。

### v1 数据的迁移

`UsageRecord::normalized()` / `TraceRecord::normalized()` 把 v1 的隐式表达（哪个字段非空 = 哪个引擎）折叠成 v2 的显式 `engine` + `client_usage`：

```
rust_usage 非空  →  engine = "rust",  client_usage = rust_usage
go_usage   非空  →  engine = "go",    client_usage = go_usage
```

在 JSONL 载入路径与 trace 读取路径调用，升级后历史数据立即出现在对比表里。

### 计费配置

```jsonc
"billing": {
  "modelPrices": {
    "claude-sonnet-4-5": {
      "inputPerMillion": 3.0,
      "outputPerMillion": 15.0,
      "cacheCreationPerMillion": 3.75,
      "cacheReadPerMillion": 0.30
    }
  },
  "upstreamMultipliers": { "9": 1.0 },   // 按凭据 id 的成本调节
  "rustMultiplier": 1.0,                  // 计费侧的引擎调节系数
  "goMultiplier": 1.0,
  "realMultiplier": 1.0,
  "nocacheMultiplier": 1.0
}
```

> `rustMultiplier` 等四个是**计费对比专用**的成本调节系数，与下发给客户端的 token 膨胀倍率是**两件不同的东西**（后者已作用在 `client_usage` 上）。

### API 响应形状

`GET /api/admin/stats/billing`：

```jsonc
{
  "points": [{
    "ts": "2026-08-01T12:00:00+08:00",
    "upstreamCost": 1.23,       // 本桶所有引擎合计
    "clientCost": 2.46,
    "calls": 42,
    "engines": [{               // 逐引擎明细
      "engine": "rust",
      "upstreamCost": 0.80,
      "clientCost": 1.60,
      "upstreamTokens": 100000,
      "clientTokens": 200000,
      "calls": 20
    }]
  }],
  "upstreamCost": 1.23,         // 全窗口汇总
  "clientCost": 2.46,
  "calls": 42,
  "engines": [ /* 同上形状 */ ]
}
```

同一条目内的两个成本来自同一批请求，可直接相除得加价倍数。

---

## 新增功能五：请求链路的计费可观测性

`trace_db.rs`（SQLite 链路库）新增 5 列：

| 列 | 类型 | 说明 |
|---|---|---|
| `engine` | TEXT | 本次请求所用引擎 |
| `upstream_usage` | TEXT (JSON) | 上游真实四元组 |
| `client_usage` | TEXT (JSON) | 客户端被计费四元组 |
| `rust_usage` / `go_usage` | TEXT (JSON) | v1 兼容列，只读不写 |

迁移是 `ALTER TABLE ADD COLUMN` 幂等实现：启动时先 `PRAGMA table_info(traces)` 读现有列，缺哪列补哪列。老库直接升级，不需要导出重建。

### 前端：单次请求的计费对比

`trace-log-page.tsx` 新增了整块计费视图（+412 行）：

- 每条请求可展开看「上游真实 / 客户端 / 差额」三列
- 按**当前**模型价格与倍率动态计算（改价格后历史请求的对比数字随之更新，因为存的是 token 而非金额）
- 未配置模型价格时显示「未定价」而非算成 0
- 支持按凭据来源、计费状态筛选，可导出

`credential-card.tsx` 新增每凭据的费用对比（+244 行）：按自定义日期段查询该凭据的上游真实 vs 客户端计费。

---

## 新增功能六：原子写文件

`src/common/fs.rs`（96 行，新增文件）—— `write_file_atomic()`。

### 要解决的问题

原版的缓存状态落盘是直接 `fs::write`。崩溃或掉电时会留下**半截文件**，下次启动解析失败 → 静默丢弃全部内容。对缓存状态的表现是「缓存莫名被重置」，且没有任何报错。

### 做法

先写同目录临时文件，再 `rename` 覆盖目标。同目录保证同一文件系统，POSIX 下 `rename` 是原子的。

Windows 上 `fs::rename` 覆盖已存在目标会失败，故失败后回退为直写 —— **至少不比原来差**。

在 tokio 运行时内调用时用 `block_in_place` 让出，避免阻塞 worker 线程。

当前使用者：`cache_metering.rs`、`cache_metering_go.rs` 的落盘路径。

---

## 功能五：可观测性扩展

### 用量记录（`usage_stats.rs`，908 → 1465 行）

原版 `UsageRecord` 只有基础字段（ts / key_id / credential_id / model / 四个 token 数 /
credits / duration / status）。二开新增 5 个字段：

| 字段 | 用途 |
|---|---|
| `is_upstream` | 是否上游凭据 —— 计费对比只统计上游 |
| `engine` | 本次请求所用引擎（`"rust"` / `"go"` / `"real"` / `"nocache"`） |
| `upstream_usage` | 上游真实四元组 |
| `client_usage` | 客户端被计费四元组 |
| `rust_usage` / `go_usage` | v1 兼容列，只读 |

聚合器（`BucketEntry`）新增三张表：

- `by_credential_model` / `by_key_credential_model` —— 支持「按凭据 × 模型」下钻
- `by_engine_billing` —— 逐引擎计费专用稀疏表

### 请求链路追踪（`trace_db.rs`，984 → 1285 行）

SQLite `traces` 表新增 5 列，全部通过 `ALTER TABLE ADD COLUMN` 幂等迁移（老库升级
不需要重建）：

```
upstream_usage    TEXT    -- 上游真实用量 JSON
rust_usage        TEXT    -- v1 兼容列
go_usage          TEXT    -- v1 兼容列
engine            TEXT    -- 引擎标识
client_usage      TEXT    -- 客户端计费用量 JSON
```

前端 `trace-log-page.tsx`（626 → 1038 行，+412）新增：

- **单次计费对比**：每条 trace 可展开看「上游真实 / 客户端 / 差额」，按当前模型价格
  与倍率**动态计算**（不落库，改价格后历史记录的对比值随之更新）
- 筛选维度：凭据来源（全部上游凭据 / 全部来源）、计费状态（有计费对比 / 未定价）
- 导出请求日志（CSV）

### 凭据卡片费用对比（`credential-card.tsx`，1184 → 1428 行）

每张上游凭据卡片内嵌一个费用对比区，逐引擎列出「上游真实 / 客户端计费」，支持
自定义日期区间查询。

---

## 功能六：原子写文件

**新增文件**：`src/common/fs.rs`（96 行）

缓存状态文件（`cache_metering.json` / go 引擎的持久化文件）原先用 `fs::write` 直写。
崩溃或掉电会留下半截文件，下次启动解析失败 → **静默丢弃全部内容**，表现为「缓存
被重置」。

`write_file_atomic()` 先写同目录 `.tmp` 再 `rename` 覆盖：

- 同目录保证同文件系统，POSIX 下 rename 原子
- Windows 上 `fs::rename` 覆盖已存在目标会失败，故回退直写（不比原来差）
- 在 tokio 运行时内用 `block_in_place` 让出，避免阻塞 worker 线程

两个引擎的持久化路径都已切换到它。

---

## Admin 界面

### 缓存引擎弹窗（新增，394 行）

`admin-ui/src/components/cache-engine-dialog.tsx`，从顶栏「缓存引擎」入口打开。
四个分节：

| 分节 | 内容 |
|---|---|
| **下发倍率** | 4×4 矩阵（4 引擎 × 4 倍率），16 个值一屏可比 |
| **缓存参数** | 只给 A / B（C / D 无缓存状态，没有 TTL / 容量可言） |
| **全局膨胀倍率** | 标注它只作为 A 未设置时的回退 |
| **运行计数器** | 只列 A / B（条目数 / 命中率 / 淘汰 / 过期） |

矩阵的行标签与提示：

```
A · rust      会话隔离前缀模拟
B · go        全局共享指纹表
C · real      上游真实 cache 划分
D · nocache   本地估算，cache 恒 0      ← cache 两列显示 —，不渲染输入框
```

A 的输入框**留空表示继承全局**，placeholder 显示实际生效值。打开弹窗时不会把全局值
填进输入框 —— 否则打开再保存就把「继承」固化成显式值，切断后续全局调整。

保存后立即热生效，无需重启；回显 sanitize 后的值，前端可提示「哪些非法值被夹取了」。

### 费用对比页（新增，220 行）

`admin-ui/src/components/billing-comparison.tsx`，逐引擎表格 8 列：

```
引擎 | 上游真实 | 客户端计费 | 差额 | 倍数 | 调用数 | Tokens (上游) | Tokens (客户端)
```

配套配置区：模型价格表（四维单价）、上游账号倍率、逐引擎计费系数。

### 客户端 Key 引擎选择（`client-keys-page.tsx`，498 → 576 行）

新建 Key 时四个引擎按钮（2×2 网格），每个附一句说明：

```
rust     隔离缓存，适合长会话
go       全局共享指纹表，不同 Key 的相同前缀会互相命中
real     直接依赖上游真实 cache 划分，用于对比基线
nocache  无本地缓存状态
```

Key 列表里非默认引擎打标签（默认 rust 不标，避免视觉噪声）。

### 顶栏膨胀倍率入口（`topbar-tools.tsx`，757 → 925 行）

「用量膨胀倍率」设置，校验区间 `[1.0, 100.0]`。界面明确标注：这里的倍率只作用于
引擎 A 未显式配置的维度，B / C / D 各有自己那套、**不受此处影响**。

---

## 配置文件完整形状

二开新增的配置全部是**增量**（零删除、零重命名），老配置文件缺任何一段都逐字段
回落默认，不会报错：

```jsonc
{
  // ── 全局膨胀倍率（引擎 A 未设置时的逐项回退）─────────────
  "inputInflationMultiplier": 1.0,
  "outputInflationMultiplier": 1.0,
  "cacheInflationMultiplier": 1.0,

  // ── 引擎 A ────────────────────────────────────────────
  "cacheEngineRust": {
    "capacity": 4096,
    "maxTtlSecs": 3600,
    "defaultTtlSecs": 300,
    "inputMultiplier": null,          // null = 继承全局
    "outputMultiplier": null,
    "cacheReadMultiplier": null,
    "cacheCreationMultiplier": null
  },

  // ── 引擎 B ────────────────────────────────────────────
  "cacheEngineGo": {
    "maxRatio": 0.85,
    "ttlSeconds": 300,
    "maxEntries": 131072,
    "minCacheableTokens": 1024,
    "opusMinCacheableTokens": 1024,
    "inputTokenMultiplier": 1.0,
    "outputMultiplier": 1.0,
    "cacheReadMultiplier": 1.0,
    "cacheCreationMultiplier": 1.0
  },

  // ── 引擎 C ────────────────────────────────────────────
  "cacheEngineReal": {
    "inputMultiplier": 1.0,
    "outputMultiplier": 1.0,
    "cacheReadMultiplier": 1.0,
    "cacheCreationMultiplier": 1.0
  },

  // ── 引擎 D ────────────────────────────────────────────
  "cacheEngineNocache": {
    "inputMultiplier": 1.0,
    "outputMultiplier": 1.0
  },

  // ── 计费对比 ──────────────────────────────────────────
  "billing": {
    "modelPrices": {
      "claude-sonnet-4-5": {
        "inputPerMillion": 3.0,
        "outputPerMillion": 15.0,
        "cacheCreationPerMillion": 3.75,
        "cacheReadPerMillion": 0.30
      }
    },
    "upstreamMultipliers": { "9": 1.0 },
    "rustMultiplier": 1.0,
    "goMultiplier": 1.0,
    "realMultiplier": 1.0,
    "nocacheMultiplier": 1.0
  }
}
```

凭据记录（`credentials.json`）新增两个字段：

```jsonc
{
  "upstreamBaseUrl": "https://api.example.com",   // 非空即判定为上游凭据
  "upstreamApiKey": "sk-..."
}
```

客户端 Key（`client_api_keys.json`）新增一个字段：

```jsonc
{ "cacheEngine": "rust" }   // 缺失时默认 rust，老文件无需迁移
```

---

## 新增 API 端点

`/v1/*` 客户端 API **零变化**（对 Claude Code 等工具完全兼容）。新增的都在 Admin：

| 方法 | 路径 | 用途 |
|---|---|---|
| GET / PUT | `/api/admin/config/token-inflation` | 全局膨胀倍率 |
| GET / PUT | `/api/admin/config/cache-engines` | 四引擎参数 + 倍率（一次原子保存） |
| GET | `/api/admin/cache-engines/stats` | A / B 的运行计数器 |
| GET / PUT | `/api/admin/config/billing` | 模型价格 + 计费系数 |
| GET | `/api/admin/stats/billing` | 逐引擎费用对比数据 |

现有端点扩展：`/api/admin/client-keys`（POST / PUT 接受 `cacheEngine`）、
`/api/admin/credentials`（接受 `upstreamBaseUrl` / `upstreamApiKey`）。

---

## 测试

| | 原版 v0.7.3 | 二开 | 增量 |
|---|---|---|---|
| `#[test]` 总数 | 537 | 658 | **+121** |

新增文件里的测试分布：

```
cache_metering_go.rs   52    引擎 B（与 Go 原版对齐的行为断言占大半）
cache_engine.rs        22    四引擎调度、倍率解析、UsageMode 语义
upstream.rs             9    上游直通的流式 / 非流式 usage 改写
cache_metering.rs      +3    引擎 A 新增的倍率与 sanitize
```

其余 ~35 个分散在 `config.rs`（配置 sanitize / 老配置兼容）、`usage_stats.rs`
（计费配对语义）、`trace_db.rs`（列迁移）、`handlers.rs`（计费快照）。

### 已修复的缺陷（均有回归测试）

| 编号 | 问题 | 回归测试 |
|---|---|---|
| P1 | 主密钥 + 上游凭据时 `input_tokens` 恒为 1 | `streaming_passthrough_keeps_real_tokens_when_simulation_disabled` |
| P2 | 引擎 B 单断点边界错误，伪造 `cache_read` | `single_breakpoint_never_reports_cache_read` |
| P6 | v1→v2 归一化方法写了但从未在生产路径调用 → 历史计费数据静默消失 | — |
| P7 | `TraceRecord::normalized` 只在文档注释里被引用，实际没实现 | — |
| P8 | 流式 `message_delta` 分支被 `usage` 存在性守卫，上游不下发 `usage` 时引擎 D 什么都不写 | `nocache_writes_local_output_even_when_upstream_omits_it` |

---

## 已知问题

**P3 — 引擎 A 失败请求污染缓存**（中）

`compute_cache_usage` 在 `begin()` 阶段就调 `cache.record()` 写入指纹表，请求失败不
回滚，后续重试可能产生幻命中。引擎 B 无此问题（两阶段，只在 `commit()` 写）。
这是原版行为，二开未改。

**P4 — 被改写的 SSE 事件丢弃非标准行**（低）

`message_start` / `message_delta` 会被重新构造成 `event:` + `data:` 两行，原有的
`id:` / `retry:` / 注释行丢失。未识别的事件类型原样透传（`_ => event_text.to_string()`），
所以影响面仅限这两类。当前 Anthropic 格式不含这些行。

**P5 — 逐引擎倍率无上限校验**（低）

全局倍率有 `[1.0, 100.0]` 校验，但四个引擎各自的倍率只校验「有限且 > 0」，可设成
10000×。

---

## 附录 A：文件清单

### 新增源文件（18 个）

| 文件 | 行数 | 内容 |
|---|---|---|
| `src/anthropic/cache_metering_go.rs` | 2270 | 引擎 B 完整实现 |
| `src/anthropic/cache_engine.rs` | 1312 | 四引擎调度层 |
| `src/anthropic/upstream.rs` | 853 | 上游直通处理器 |
| `src/common/fs.rs` | 96 | 原子写文件 |
| `admin-ui/src/components/cache-engine-dialog.tsx` | 394 | 缓存引擎弹窗 |
| `admin-ui/src/components/billing-comparison.tsx` | 220 | 费用对比页 |
| `admin-ui/src/components/ui/table.tsx` | 117 | 表格组件 |
| `admin-ui/src/api/billing.ts` | 40 | 计费 API 客户端 |
| `admin-ui/src/hooks/use-cache-engines.ts` | 38 | 引擎配置 hook |
| `admin-ui/src/hooks/use-billing.ts` | 33 | 计费 hook |

文档：`FEATURES.md`(791) / `BILLING_SCHEMA_V2.md`(173) / `PLAN-four-engines.md`(230)

### 改动最大的既有文件

| 文件 | 原版 → 二开 | 增量 |
|---|---|---|
| `src/model/config.rs` | 432 → 1124 | +692 |
| `src/anthropic/handlers.rs` | 2116 → 2807 | +691 |
| `src/admin/usage_stats.rs` | 908 → 1465 | +557 |
| `admin-ui/src/components/trace-log-page.tsx` | 626 → 1038 | +412 |
| `src/kiro/token_manager.rs` | 5344 → 5738 | +394 |
| `src/admin/handlers.rs` | 1612 → 2003 | +391 |
| `src/admin/service.rs` | 3451 → 3768 | +317 |
| `src/admin/trace_db.rs` | 984 → 1285 | +301 |
| `src/kiro/provider.rs` | 1023 → 1287 | +264 |
| `admin-ui/src/components/credential-card.tsx` | 1184 → 1428 | +244 |
| `src/anthropic/cache_metering.rs` | 1393 → 1630 | +237 |

**零文件删除、零函数删除**（逐个比对 `pub fn` 清单确认）。

---

## 附录 B：本文档的核实方法

本文档的每个数字都来自与原版 **v0.7.3** 源码树的机械比对，不是凭记忆或推测：

```bash
# 1. 从原版 git 仓库导出 v0.7.3 的完整树（二开的分叉点）
cd kiro.rs && git archive v0.7.3 | tar -x -C /tmp/orig073

# 2. 逐文件 SHA-256 比对，分出「新增 / 删除 / 修改 / 相同」
#    → new=18  removed=0  changed=41  identical=121

# 3. 逐项提取符号差集：
#    - config 字段：serde 反序列化后取 JSON 键名（不靠读代码猜 camelCase）
#    - struct / enum / pub fn：正则提取后做集合差
#    - 路由：从 router.rs 提取路径字面量
#    - SQLite 列：从 SCHEMA 常量与迁移数组提取
```

**为什么必须锁定 v0.7.3**：原版现已到 v0.7.4。直接拿二开对比原版 master，会把
「原版 0.7.3→0.7.4 的新增」也算成二开的差异。第一次比对时 `token_manager.rs` 显示
**-785 行**（最大单项，且是减少），正是这个原因 —— 锁定同版本后该文件实际是 **+394**。

**FEATURES.md 的一处表述已据此更正**：它把「模拟缓存 token 膨胀」整体列为二开新增，
但 `compute_cache_usage` / `CacheUsage` / `split_against_total` / `isolation_seed` 在
v0.7.3 已存在且已下发给客户端（`handlers.rs:1257`、`stream.rs:1420`）。二开新增的是
**倍率体系**（原版 `inflation_multiplier` 零处）、**引擎 B/C/D**、**引擎选择**、
**上游直通**、**逐引擎计费**。
